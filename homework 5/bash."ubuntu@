fidansaid:~/workspace (master) $ ls
homework 5/  homework6/  homework7/
fidansaid:~/workspace (master) $ wget
wget: missing URL
Usage: wget [OPTION]... [URL]...

Try `wget --help' for more options.
fidansaid:~/workspace (master) $ date +'%m-%d-%y'
10-03-16
fidansaid:~/workspace (master) $ date +'%r'
06:40:27 AM
fidansaid:~/workspace (master) $ history
    1  mkdir
    2  nano demo.html
    3  git add 
    4  history
    5  git init
    6  git add index.html
    7  comitt -m "work it"
    8  git comitt -m "work it"
    9  git remote add origin https://github.com/FidanSaid/Hom6.git
   10  git push
   11  FidanSaid
   12  get_crtime() {    for target in "${@}"; do        inode=$(stat -c %i "${target}");        fs=$(df "${target}"  | tail -1 | awk '{print $1}');        crtime=$(sudo debugfs -R 'stat <'"${inode}"'>' "${fs}" 2>/dev/null | grep -oP 'crtime.*--\s*\K.*');        printf "%s\t%s\n" "${target}" "${crtime}";    done; }
   13  cls
   14  <clear
   15  alias
   16  wget
   17  wget www.google.com
   18  history
   19  clean
   20  history|cls|wget|alias|clear
   21  clear
   22  date +"%m-%d-%Y"
   23  clear
   24  date %m-%d-%y
   25  date +'%m/%d/%Y'
   26  date +'%r'
   27  10/03/2016
   28  time-admin
   29  date +"%T"
   30  date +"%c"
   31  date +" %r "
   32  history
   33  ls
   34  wget
   35  date +'%m-%d-%y'
   36  date +'%r'
   37  history
fidansaid:~/workspace (master) $ history|wget|clear|ls|alias|date +'%m-%d-%y